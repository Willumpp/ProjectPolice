using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyCollisionStop : MonoBehaviour
{
    /*
    EnemyMovement script;
    public GameObject self;

    void Start()
    {
        script = self.GetComponent<EnemyMovement>();
    }

    void OnTriggerEnter(Collider collision)
    {
        //Tell player to stop when touching a collidable object
        if (collision.gameObject.CompareTag("CollidableTerrain") || collision.gameObject.CompareTag("Player"))
        {
            script.ChangeMovement(true);
        }
        else
        {
            script.ChangeMovement(false);
        }
    }
    */
}
